package control;

import util.Event;
import util.ExchangeCommands;

public class EventProcess {
	public static Object processExchangeCommand(ExchangeCommands in) {
		switch (in) {
		case QUIT:
			//return MProcess.process(new hQuit(), new PQuit().produce());
			return null;
		case PUTCHESS:
			return null;
		default:
			return null;
		}
	}
}
